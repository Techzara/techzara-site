{% extends 'backoffice/base_admin.html.twig' %}

{% form_theme edit_form 'form/fields.html.twig' %}

{% block title %}{{ parent() }} Modification Slide {% endblock %}

{% set menu_slide        = true %}
{% set menu_slide_create = true %}

{% block body %}
    {% for flashMessage in app.session.flashbag.get('success') %}
        <div class="callout callout-success">
            {{ flashMessage }}
        </div>
    {% endfor %}
    {% for flashMessage in app.session.flashbag.get('error') %}
        <div class="callout callout-danger">
            {{ flashMessage }}
        </div>
    {% endfor %}

    <div class="row">
        <div class="col-md-12">
            <div class="box box-primary">
                {{ form_start(edit_form) }}
                    <div class="box-header">
                        <h3 class="box-title">Editer un slide</h3>
                    </div>

                    <div class="box-body">
                        {{ form_row(edit_form.sldFirstTitle) }}
                        {{ form_row(edit_form.sldSecondTitle) }}
                        {{ form_row(edit_form.sldThirdTitle) }}
                        {{ form_row(edit_form.sldImageUrl) }}
                        <div class="form-group">
                            <div class="row">
                                {% if slide.sldImageUrl %}
                                    <div class="col-md-2 col-lg-2 col-xs-2 blc-image">
                                        <div class="image-container-block">
                                            <img class="img-vignette img-responsive" src="{{ slide.sldImageUrl }}" />
                                        </div>
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>

                    <div class="box-footer">
                        <input type="submit" class="btn btn-primary" value="Modifier" name="update_slide"/>
                        <a href="{{ path('slide_index') }}"
                           class="btn btn-default pull-left btn-mini-rectif" >
                            Retour à la liste
                        </a>
                    </div>
                {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
{% endblock %}