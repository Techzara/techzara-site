{% extends 'front_site_office/base_front.html.twig' %}

{% set _locale = app.request.locale %}

{% block title %}{{ parent() }} {{ cms.translate(_locale).cmstTitle }} {% endblock %}

{% block body %}
    <div class="container">
        <div class="list-count text-center kl-title-bloc">
            <h2 class="kl-title-page">{{ cms.translate(_locale).cmstTitle }}</h2>
        </div>
        <div class="">
            {{ cms.translate(_locale).cmstContent|raw }}
        </div>
    </div>
{% endblock %}